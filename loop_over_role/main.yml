- name: loop over role playbook
  hosts: localhost
  tasks:

    - name: loop over role task
      include_role:
        name: myrole
      with_items:
        - test
      loop_control:
        loop_var: thing

